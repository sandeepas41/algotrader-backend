package com.algotrader.domain.enums;

/**
 * Direction of a position, derived from the signed quantity.
 * Positive quantity = LONG, negative quantity = SHORT.
 */
public enum PositionType {
    LONG,
    SHORT
}
